# Implementation Plans

Generated by the improve skill on 2026-06-10 (quick audit, planned at commit
`0cdc229`). Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

Context note: at planning time the working tree carried uncommitted maintainer
changes in `src-tauri/src/hotkey_listener.rs`, `src-tauri/src/lib.rs`,
`src-tauri/src/platform/macos.rs`, and `src-tauri/tauri.conf.json`. Plans were
written to avoid those regions; every plan has a drift check — run it.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Run whisper inference on a blocking thread | P1 | S | — | DONE |
| 002  | Add network timeouts to model downloads | P1 | S | — | DONE |
| 003  | Small hardening: path-aware transcriber init, panic-free get_app_state, single-open mic test | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies. The plans touch disjoint files by design
  (001 → `hotkey.rs`; 002 → `model.rs`; 003 → `transcribe.rs`, `lib.rs`,
  `audio.rs`), so they can be executed in any order or in parallel worktrees.

## Findings considered and rejected

- **`main.ts` god module (2,922 lines, zero frontend tests)**: real and the
  largest debt item, but deliberately not planned in this round at the
  maintainer's choice. If picked up later: write characterization tests
  FIRST, then split — do not refactor unprotected.
- **Stats queries mix UTC and localtime windows** (`db.rs:446-470`): cosmetic
  inconsistency in a stats panel; not worth a plan.
- **FTS5 injection**: already properly sanitized (`db.rs:35`,
  `sanitize_fts5_query`); no action needed.
- **Security sweep overall**: clean — consistent `escapeHtml` on all user data
  in `innerHTML` templates, parameterized SQL, SHA-256 + magic-byte model
  verification, signed Tauri updater. Do not re-audit these on the next run
  unless those areas change.

## Not audited in this quick pass

Platform layers (`platform/macos.rs`, `platform/windows.rs`),
`hotkey_listener.rs`, `onboarding.ts` logic, performance, dependency audits
(`cargo audit` / `bun audit`), docs.
