{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "indicator",
  "description": "Indicator window capabilities for the Fing app",
  "windows": ["indicator"],
  "permissions": ["core:default"]
}
