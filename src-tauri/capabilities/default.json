{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Fing app",
  "windows": ["main", "indicator"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default"
  ]
}
